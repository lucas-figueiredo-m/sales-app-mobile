import Font from '@salesapp/theme/fonts';
import Layout from '@salesapp/theme/layout';
import Spacing from '@salesapp/theme/spacing';

export const useTheme = () => {
  return {
    Font,
    Layout,
    Spacing,
  };
};
